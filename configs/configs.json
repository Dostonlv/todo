{
    "server": {
        "port": ":3000",
        "host": "http://127.0.0.1"
    },
    "logger": {
        "level": "debug",
        "filename": "./app.log"
    },
    "database": {
        "migration": "postgres://postgres:postgres@localhost:5432/stol_db?sslmode=disable",
        "casbin": "postgres://postgres:postgres@localhost:5432/stol_db?sslmode=disable",
        "dns": "user=postgres password=postgres dbname=stol_db host=localhost port=5432"
    }
}